import { NameShowerPipe } from './name-shower.pipe';

describe('NameShowerPipe', () => {
  it('create an instance', () => {
    const pipe = new NameShowerPipe();
    expect(pipe).toBeTruthy();
  });
});
