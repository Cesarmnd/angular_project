import { BooleanStyleDirective } from './boolean-style.directive';

describe('BooleanStyleDirective', () => {
  xit('should create an instance', () => {
    // const directive = new BooleanStyleDirective();
    // expect(directive).toBeTruthy();
  });
});
